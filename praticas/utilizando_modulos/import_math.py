import math            # importa toda a biblioteca
from math import sqrt  # importa apenas a respectiva funcionalidade

num = int(input("Digite um número: "))
raiz = sqrt(num)

print(f"A raiz quadrada de {num} é {raiz:.2f}")